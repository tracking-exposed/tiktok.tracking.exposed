pipeline:
  build:
    image: klakegg/hugo:ext-alpine-ci
    commands:
      - hugo
      - rm -rf /site/*
      - mv -f public/* /site
    volumes:
      - /var/www/sites/tiktok.tracking.exposed:/site

branches: 
  - main
